{
  "name": "logseq-callout",
  "description": "Tag-driven callout styling for Logseq blocks — icons, labels, colored backgrounds with nested visual hierarchy",
  "version": "0.1.0",
  "author": "Danzu",
  "license": "MIT",
  "theme": false,
  "web": true,
  "supportsDB": true,
  "supportsDBOnly": true,
  "effect": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "serve": "vite serve",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@logseq/libs": "^0.2.12"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "terser": "^5.46.0",
    "typescript": "^5.3.0",
    "vite": "^7.2.2"
  },
  "logseq": {
    "id": "logseq-danzu-callout",
    "title": "Callout Manager",
    "description": "Tag-driven callout styling — icons, labels, colored backgrounds with nested visual hierarchy",
    "icon": "./icon.svg",
    "main": "dist/index.html"
  }
}
